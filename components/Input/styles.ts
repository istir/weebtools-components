import { createStyle, MIN_CLICKABLE_SIZE } from "../../styles/theme";

export const styles = createStyle({
  input: {
    background: "none",
    minHeight: "inherit",
    outline: "none",
    borderWidth: 0,
    color: "inherit",
  },
});
